{% block body %}
    {{ include('header.html.twig') }}
    <link href="{{ asset('css/addcategory.css') }}" rel="stylesheet" />
    {{ form_start(form) }}
    {{ form_row(form.newfile) }}
    <input type="submit" name="submit" class="form-adding" value="Send">
    {{ form_end(form) }}

    {{ include('footer.html.twig') }}
{% endblock %}
